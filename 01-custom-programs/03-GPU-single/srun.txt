# compile the GPU program
srun -p L40 --container-name=<CONTAINER> --pty --gpus=1 --container-workdir=${PWD} make
# run the GPU program
srun -p L40 --container-name=<CONTAINER> --pty --gpus=1 --container-workdir=${PWD} ./prog 0 $((2**13)) 1
